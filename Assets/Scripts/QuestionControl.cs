using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QuestionControl : MonoBehaviour
{
    public GameObject Panel;

    public void NextQuestion_1()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q1");
                animator.SetBool("Q1", !isOpen);
            }
        }
    }
    public void NextQuestion_2()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q2");
                animator.SetBool("Q2", !isOpen);
            }
        }
    }
    public void NextQuestion_3()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q3");
                animator.SetBool("Q3", !isOpen);
            }
        }
    }
    public void NextQuestion_4()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q4");
                animator.SetBool("Q4", !isOpen);
            }
        }
    }
    public void NextQuestion_5()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q5");
                animator.SetBool("Q5", !isOpen);
            }
        }
    }
    public void NextQuestion_6()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q6");
                animator.SetBool("Q6", !isOpen);
            }
        }
    }
    public void NextQuestion_7()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q7");
                animator.SetBool("Q7", !isOpen);
            }
        }
    }
    public void NextQuestion_8()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q8");
                animator.SetBool("Q8", !isOpen);
            }
        }
    }
    public void NextQuestion_9()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q9");
                animator.SetBool("Q9", !isOpen);
            }
        }
    }
    public void NextQuestion_10()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q10");
                animator.SetBool("Q10", !isOpen);
            }
        }
    }
    public void NextQuestion_11()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q11");
                animator.SetBool("Q11", !isOpen);
            }
        }
    }
    public void NextQuestion_12()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q12");
                animator.SetBool("Q12", !isOpen);
            }
        }
    }
    public void NextQuestion_13()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q13");
                animator.SetBool("Q13", !isOpen);
            }
        }
    }
    public void NextQuestion_14()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q14");
                animator.SetBool("Q14", !isOpen);
            }
        }
    }
    public void NextQuestion_15()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q15");
                animator.SetBool("Q15", !isOpen);
            }
        }
    }
    public void NextQuestion_16()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q16");
                animator.SetBool("Q16", !isOpen);
            }
        }
    }
    public void NextQuestion_17()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q17");
                animator.SetBool("Q17", !isOpen);
            }
        }
    }
    public void NextQuestion_18()
    {
        if (Panel != null)
        {
            Animator animator = Panel.GetComponent<Animator>();
            if (animator != null)
            {
                bool isOpen = animator.GetBool("Q18");
                animator.SetBool("Q18", !isOpen);
            }
        }
    }
}
